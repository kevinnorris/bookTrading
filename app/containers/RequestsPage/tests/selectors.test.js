// import { fromJS } from 'immutable';
// import { makeSelectRequestsPageDomain } from '../selectors';

// const selector = makeSelectRequestsPageDomain();

describe('makeSelectRequestsPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
