// import { fromJS } from 'immutable';
// import { makeSelectMyBooksPageDomain } from '../selectors';

// const selector = makeSelectMyBooksPageDomain();

describe('makeSelectMyBooksPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
