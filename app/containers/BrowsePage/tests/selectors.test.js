// import { fromJS } from 'immutable';
// import { makeSelectBrowsPageDomain } from '../selectors';

// const selector = makeSelectBrowsPageDomain();

describe('makeSelectBrowsePageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
