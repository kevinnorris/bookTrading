// import { fromJS } from 'immutable';
// import { makeSelectWishlistPageDomain } from '../selectors';

// const selector = makeSelectWishlistPageDomain();

describe('makeSelectWishlistPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
