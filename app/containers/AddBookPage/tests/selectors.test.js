// import { fromJS } from 'immutable';
// import { makeSelectAddBookPageDomain } from '../selectors';

// const selector = makeSelectAddBookPageDomain();

describe('makeSelectAddBookPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
